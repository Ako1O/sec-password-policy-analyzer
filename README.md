# sec-password-policy-analyzer
Structure of the program will be the next one:
1. Ask for password input
2. Run checks:
   - length_ok = ...
   - has_upper = ...
   - has_lower = ...
   - has_digit = ...
   - has_special = ...
3. Add any company "standart" verification rules
4. Compute score
5. Print results
